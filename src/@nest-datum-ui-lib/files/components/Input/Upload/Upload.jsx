import React from 'react';

let Upload = () => {
	return <React.Fragment>
		Upload
	</React.Fragment>;
};

Upload = React.memo(Upload);
Upload.propTypes = {
};
Upload.defaultProps = {
};

export default Upload;
