import React from 'react';

let Reset = () => {
	return <React.Fragment>
		Reset
	</React.Fragment>;
};

Reset = React.memo(Reset);
Reset.defaultProps = {
};
Reset.propTypes = {
};

export default Reset;
